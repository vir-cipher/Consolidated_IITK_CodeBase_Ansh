# crypto/ — image steganography
Hide a short text message in the least-significant bits of a PNG so the picture looks unchanged.
Run: `pip install pillow` then `python steganography.py`.
Make it mine (TODO): add a password, show how many characters fit, add a decode script.
Inspired by github.com/nitiksh/Steganography-with-Python-Programming (my own code).
